package cmd

var (
	source []string // the list of source files
	format string   // the target format
	output string   // the output dir of target files

	srcFormat string // the format of the source files
	srcDir    string // the directory of source files
)
